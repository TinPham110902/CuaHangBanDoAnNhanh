@model IEnumerable<DoAnNhanh.Models.Log_login>

@{
    ViewBag.Title = "LsLogin";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@{
    var user = (DoAnNhanh.Models.Tk)HttpContext.Current.Session["user"];
}


<p>
    @Html.ActionLink("Back To List", "Index")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Login)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Logout)
        </th>
       
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Login)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Logout)
            </td>


        </tr>

    }

</table>
