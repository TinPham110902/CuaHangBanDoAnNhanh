@model IEnumerable<DoAnNhanh.Models.Tk>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Danh Sách Tài Khoản</h2>
@*@{ 
    var user = (DoAnNhanh.Models.Tk)HttpContext.Current.Session["user"];
}
<h2>@user.Tk1</h2>
<h2>@user.Quyen.TenQuyen</h2>
<p>*@
    @Html.ActionLink("Thêm Mới", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Tên Tài Khoản
        </th>
        <th>
         Tên Quyền
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Tk1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Quyen.TenQuyen)
        </td>
        <td>
            @Html.ActionLink("Lịch sử đăng nhập", "LsLogin", new { id = item.Tk1.ToString().Trim() }) |

            @Html.ActionLink("Chỉnh sửa", "Edit", new { id = item.Tk1.ToString().Trim() }) |

            @Html.ActionLink("Xóa", "Delete", new { id = item.Tk1.ToString().Trim() })
        </td>
    </tr>
}

</table>
